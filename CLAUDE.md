# 데일리카네기 (Daily Carnegie) 웹사이트

> 이 프로젝트를 처음 보는 사람(및 Claude)이 빠르게 이해하도록 정리한 안내서입니다.

## 한줄 요약
**데일리카네기** = "매일 만나는 카네기 교육"이라는 뜻의 바인그룹 서비스 브랜드. 데일카네기코리아(바인그룹)의 **최고경영자(CEO) 과정** 안내·입학 상담 사이트. **순수 정적 사이트**(HTML/CSS/JS)이며 프레임워크·서버·DB가 없습니다. GitHub Pages로 배포됩니다.

## 기본 정보
- **저장소**: github.com/x26589334-cpu/daily-carnegie
- **배포 URL**: https://dailycarnegie.com/ (커스텀 도메인 연결 시 `CNAME` 파일 추가 + 모든 페이지의 canonical/og:url + `sitemap.xml` + `robots.txt`의 URL 교체 필요)
- **호스팅/배포**: GitHub Pages. `main` 브랜치에 **push하면 1~2분 뒤 자동 배포**됩니다. (빌드 과정 없음)
- **기술**: 정적 HTML/CSS/JS. 폰트는 Pretendard(jsdelivr CDN) + Noto Serif KR(Google Fonts, 헤드라인용). 지도는 Google Maps iframe(API 키 없음).
- **디자인 톤**: 딥 네이비(#0B1F3A) × 골드(#B8933F) × 아이보리. 세리프 헤드라인. "중후하고 고급스러운" CEO 교육 톤.

## 파일 구조
- `index.html` — 홈 (히어로에 **모집 중인 기수 카드** 포함)
- `program.html` — 과정 소개 (12주 커리큘럼·특징·기대효과·특전)
- `about.html` — 데일카네기 소개 (철학·수치·연혁·프로그램·고객사)
- `alumni.html` — 카네기클럽 동문 네트워크 + 후기
- `faq.html` — FAQ (아코디언, FAQPage JSON-LD)
- `apply.html` — 개강 일정표·수강료 + **입학 상담 신청 폼** (지도·오시는 길 없음)
- `privacy.html` — 개인정보처리방침 (noindex)
- `assets/css/style.css` — 전체 스타일. 색상·폰트는 상단 `:root` 디자인 토큰에서 관리
- `assets/js/main.js` — 공통 JS (모바일 카테고리 바, FAQ 아코디언, 스크롤 reveal, `[data-count]` 카운트업, `form[data-sheet]` 전송, 전화 플로팅 버튼, 현재 메뉴 active)
- `assets/images/favicon.svg` — 파비콘 (네이비 원 + 골드 C). 로고 이미지 없음 — 헤더/푸터 로고는 CSS 모노그램(`.logo .mark`)
- `google-apps-script.gs` — 상담폼 백엔드(구글 Apps Script) **참고용 사본** (연결 방법 주석 포함)
- `sitemap.xml`, `robots.txt`, `.nojekyll`

## 공통 마크업
헤더·푸터는 모든 페이지에 **복사되어** 있습니다(인클루드 없음). 메뉴를 바꾸면 7개 HTML 모두 수정해야 합니다.
메뉴: 과정 소개 / 데일카네기 / 동문·후기 / FAQ / 일정·신청 + 우측 전화번호·"입학 상담 신청" 버튼.

## 외부 연동
- **상담 신청 폼**(`apply.html`): `<form data-sheet="…/exec">` — 과외(perfectedu)·픽포스와 **같은 공용 Apps Script 웹앱** → 구글 "웹 문의" 시트. hidden `sheet=데일리카네기`, `_form=데일리카네기-입학상담`(구분 컬럼), `main.js`가 `_page`/`_time` 추가. 필드명은 시트 헤더가 되므로 **한글**: 성함, 연락처, 회사명, 직책, 이메일, 관심과정, 문의내용, 개인정보동의. 전용 탭으로 분리하려면 `google-apps-script.gs` 새 웹앱 배포 후 URL 교체.
- **전화**: 다른 사이트와 같은 마케팅 상담 번호 **010-6832-1994** (헤더·푸터·플로팅 버튼·CTA·FAQ). 데일카네기코리아의 사무실 전화/이메일/주소/지도는 **사이트에 넣지 않음** (사무실 방문 없이 온라인 상담·등록).
  - ⚠️ **지사 담당자 개인 휴대폰은 절대 넣지 않음.** 지사별 포스터에는 지사장 개인번호가 찍혀 있으나, 상담 창구는 위 통합번호 하나로 유지한다 (지사끼리 영업 경쟁 관계).
  - 교육 **장소**는 2026-09-04부터 지역별 실제 장소·주소를 `apply.html` 일정표에 표기한다(이전의 "서울 (상담 시 안내)" 단일 표기 정책은 폐기). 장소가 확정 안 된 지역만 "상담 시 안내".
- **네이버 서치어드바이저**: 소유확인 메타태그를 `index.html` `<head>`에 삽입 완료(2026-08-29). **GA4 / 구글 서치콘솔은 아직 미삽입.**
- **HTTPS**: 2026-09-04 확인 시 정상 동작(http → https 301, 전 페이지 200). 이전에 기록됐던 "인증서 미발급" 문제는 해소됨.

## 콘텐츠 출처 (2026-08 기준)
- 과정 정보(12주 커리큘럼, 특징, 특전, 시간, 서울 103기 2026.9.8~11.24 화 17:00~21:00)는 carnegie.co.kr 공개 정보. 공식 전화·이메일·주소는 사이트에 표기하지 않음.
- **수강료는 지역마다 다르다** (2026 하반기 기준): 지역 CEO 과정 대부분 **2,900,000원**, 서울 4,000,000원, 전북 3,800,000원(16주), 울산 2,750,000원, 창원 2,500,000원, 포항 1,980,000원. 이전에 전 지역 4,000,000원으로 표기돼 있던 것은 오류였고 2026-09-04에 바로잡음. 포스터에 인쇄되어 공개되는 금액이라 사이트 표기에 문제 없음.
- **2026 하반기 일정의 출처와 검증**(2026-09-04): 본사 통합 일정표 + `event/` 폴더의 지사 포스터를 대조해 넣었다. 본사 표의 **개강일 컬럼에 오류가 여러 건** 있었고(12주 과정인데 9~10주, 요일이 실제 날짜와 불일치), 포스터와 수료일 역산이 서로 일치한 **부산 78기(9.8) · 파주 27기(9.2) · 광명 57기(9.16) · 울산 65기(9.3)** 는 포스터 쪽을 채택했다.
  - ⚠️ **광주·하남 52기, 대전 53기, 광주 64기, 대구 88기, 진주 75기**는 표에 오류가 있는데 대조할 포스터가 없어 **일정 미확정** 상태로 넣었다. 본사 확인 후 `data-start`/`data-end`를 채울 것.
  - 고양은 본사 표 **55기** / 포스터 **54기**로 기수 번호가 엇갈린다(날짜는 동일). 표를 따랐다.
- 수치(1912년 설립, 85개국, 200개+ 오피스, **25개+ 교육 언어**, 1,000만 명 수료, 연 5,000명 국내 수료, 4만 명 카네기클럽, 3,000개 기업, 만족도 90%, 1998년 ISO 9001, ACE 학점 인증)는 carnegie.co.kr 소개 페이지 기준. **전국 8개 지사 / CEO 과정 40개 도시 3만 명 배출**은 carnegie.co.kr 최고경영자 과정 페이지 기준.
- `about.html`의 **창립자 섹션·대표 저서 섹션**(1888 출생, 1908 워런스버그 졸업, 1911 세일즈 은퇴, 1912 125번가 YMCA, 1936 인간관계론, 1948 자기관리론, 1932 링컨 이야기, 1955 별세·수료생 45만 명)은 영문 위키백과 Dale Carnegie 문서 기준.
- ⚠️ 고객사 로고 스트립(삼성·현대·SK·LG·Google·IBM·Microsoft·Coca-Cola)은 **carnegie.co.kr에서 재확인되지 않았습니다.** 실제 고객사 명단으로 교체하거나 삭제 필요.
- **후기 6건과 "소수 정원", "저녁 식사 포함", 신청 절차, 104기 일정, 보강 규정은 예시/추정**입니다. 실제 정보로 교체 필요.

## 자주 하는 작업 가이드
- **새 기수 반영**: `apply.html`의 `#class-table` **표 한 곳만** 고친다.
  - 각 `<tr>`의 `data-start="YYYY-MM-DD"` / `data-end="YYYY-MM-DD"` 를 보고 `main.js`가 상태 뱃지(모집 중 / 마감 임박 D-n / 오늘 개강 / 진행 중 / 종료)를 **오늘 날짜 기준으로 자동 계산**한다. 뱃지 글자는 손대지 말 것 — 비워두면 JS가 채운다.
  - 종료된 기수는 자동으로 숨겨지고, 진행 중인 기수는 표 아래쪽으로 자동 정렬된다.
  - `data-start`가 **없는** 행은 "일정 문의"로 표시된다(일정 미확정 지역용).
  - `data-area`는 지역 필터 값: `수도권` / `영남` / `충청·호남`.
  - **홈 히어로 카드**(`index.html`의 `.class-card[data-auto-next]`)는 `main.js`가 `apply.html`을 fetch 해서 **가장 임박한 기수로 자동으로 채운다.** 그래서 홈은 따로 고칠 필요가 없다. HTML에 적힌 내용은 JS 실패 시 보이는 기본값이므로 서울 기수만 가끔 맞춰주면 된다.
  - 폼의 `select[name="관심과정"]` 옵션은 자동이 아니므로 기수 갱신 시 같이 고칠 것. **필드명 `관심과정`은 구글 시트 헤더라 절대 바꾸지 말 것.**
- **캐시 갱신**: CSS/JS 수정 시 각 HTML의 `style.css?v=…`, `main.js?v=…` 버전 문자열을 올릴 것.
- **새 페이지 추가**: HTML 만들고 → 헤더/푸터 메뉴 7개 파일 반영 → `sitemap.xml`에 URL 추가.

## ⚠️ 주의사항
- **공개 저장소**입니다. 신청자 데이터(xlsx/csv), 시트 ID 외 비밀값(봇 토큰 등)은 절대 커밋 금지 (`.gitignore` 등록됨).
- **브랜드 표기 규칙**: 서비스명(로고·title·og:site_name·푸터·저작권)은 **"데일리카네기"**(Daily Carnegie, "매일 만나는 카네기 교육"). 교육 과정·역사·수료증 등 내용을 가리킬 때는 **"데일카네기"**(공식 표기) 그대로. 둘을 섞지 말 것.
